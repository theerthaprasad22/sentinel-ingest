# Web layer
fastapi>=0.115,<0.117
uvicorn[standard]>=0.34,<0.36
jinja2>=3.1,<4
pydantic>=2.10,<3

# Ingestion
httpx[http2]>=0.28,<0.29
lxml>=5.3,<6
# A browser advertises br and zstd. Claiming them in Accept-Encoding without
# being able to decode them yields silent garbage, not an error -- Remotive
# serves brotli and the parser saw zero rows until these were installed.
brotli>=1.1
zstandard>=0.23

# ML layer (soft-block classifier, drift stats, semantic index, weak-supervision tagger)
numpy>=2.1,<3
scipy>=1.14,<2
scikit-learn>=1.6,<2
